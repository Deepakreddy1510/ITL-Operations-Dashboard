"use client";import {Shell} from "@/components/shell";import {useState} from "react";
export default function Page(){const [msg,setMsg]=useState("");async function upload(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);const r=await fetch("/api/import",{method:"POST",body:form});const j=await r.json();setMsg(r.ok?`Imported ${j.count} rows.`:j.error)}return <Shell title="Import Data" subtitle="Upload simple CSV or Excel files"><form onSubmit={upload} className="card max-w-2xl p-6"><label className="text-sm font-medium">Data type</label><select name="type" className="input mt-2 w-full"><option value="customers">Customers</option><option value="inventory">Inventory</option></select><label className="mt-5 block text-sm font-medium">CSV or Excel file</label><input name="file" type="file" accept=".csv,.xlsx,.xls" required className="mt-2 block w-full text-sm"/><button className="mt-6 rounded-xl bg-slate-900 px-5 py-2.5 text-sm font-medium text-white">Import file</button>{msg&&<p className="mt-4 text-sm">{msg}</p>}<div className="mt-6 rounded-xl bg-slate-50 p-4 text-sm text-slate-600 dark:bg-slate-950 dark:text-slate-300"><b>Customer columns:</b> name, phone, location, outstanding<br/><b>Inventory columns:</b> name, currentStock, minimumStock, unit, supplier</div></form></Shell>}
