"use client"; import {useState} from "react"; import {useRouter} from "next/navigation";
export default function Login(){const r=useRouter();const [e,setE]=useState("");const [p,setP]=useState("");const [err,setErr]=useState("");async function submit(ev:React.FormEvent){ev.preventDefault();setErr("");const res=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:e,password:p})});if(res.ok)r.push("/dashboard");else setErr("Invalid email or password")}return <main className="grid min-h-screen place-items-center p-5"><form onSubmit={submit} className="card w-full max-w-sm p-7"><div className="mb-6"><div className="mb-4 grid h-12 w-12 place-items-center rounded-2xl bg-slate-900 font-bold text-white">NP</div><h1 className="text-2xl font-semibold">Manager sign in</h1><p className="mt-1 text-sm text-slate-500">Hyderabad branch dashboard</p></div><label className="text-sm">Email</label><input className="input mt-1 w-full" value={e} onChange={x=>setE(x.target.value)} type="email" required/><label className="mt-4 block text-sm">Password</label><input className="input mt-1 w-full" value={p} onChange={x=>setP(x.target.value)} type="password" required/>{err&&<p className="mt-3 text-sm text-red-600">{err}</p>}<button className="mt-5 h-11 w-full rounded-xl bg-slate-900 font-medium text-white">Sign in</button><p className="mt-4 text-xs text-slate-500">Use an approved account created by the administrator.</p></form></main>}
