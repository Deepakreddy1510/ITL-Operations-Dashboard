import {NextResponse} from "next/server";import {z} from "zod";import {prisma} from "@/lib/prisma";const schema=z.object({name:z.string().min(2),phone:z.string().optional().nullable(),location:z.string().optional().nullable(),outstanding:z.coerce.number().min(0)});
export async function PUT(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:"Invalid customer data"},{status:400});return NextResponse.json(await prisma.customer.update({where:{id:Number(id)},data:p.data}))}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;try{await prisma.customer.delete({where:{id:Number(id)}});return NextResponse.json({ok:true})}catch{return NextResponse.json({error:"Delete related quotations/orders first"},{status:400})}}
