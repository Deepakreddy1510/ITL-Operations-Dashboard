import {NextResponse} from "next/server";import {z} from "zod";import {prisma} from "@/lib/prisma";const schema=z.object({number:z.string().min(2),customerId:z.coerce.number().int(),date:z.coerce.date(),amount:z.coerce.number().min(0),salesperson:z.string().min(2),status:z.enum(["PENDING","ACCEPTED","REJECTED","COMPLETED"]),notes:z.string().optional().nullable()});
export async function PUT(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:"Invalid quotation data"},{status:400});return NextResponse.json(await prisma.quotation.update({where:{id:Number(id)},data:p.data,include:{customer:true}}))}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;await prisma.quotation.delete({where:{id:Number(id)}});return NextResponse.json({ok:true})}
