import {NextResponse} from "next/server";import {z} from "zod";import {prisma} from "@/lib/prisma";const schema=z.object({name:z.string().min(2),currentStock:z.coerce.number().min(0),minimumStock:z.coerce.number().min(0),unit:z.string().min(1),supplier:z.string().min(2),lastPurchaseDate:z.coerce.date().optional().nullable()});
export async function GET(){return NextResponse.json(await prisma.material.findMany({orderBy:{name:"asc"}}))}
export async function POST(req:Request){const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:"Invalid material data"},{status:400});try{return NextResponse.json(await prisma.material.create({data:p.data}),{status:201})}catch{return NextResponse.json({error:"Material already exists"},{status:409})}}
