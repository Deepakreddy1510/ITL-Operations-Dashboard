import {NextResponse} from "next/server";import {z} from "zod";import {prisma} from "@/lib/prisma";const schema=z.object({name:z.string().min(2),currentStock:z.coerce.number().min(0),minimumStock:z.coerce.number().min(0),unit:z.string().min(1),supplier:z.string().min(2),lastPurchaseDate:z.coerce.date().optional().nullable()});
export async function PUT(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:"Invalid material data"},{status:400});return NextResponse.json(await prisma.material.update({where:{id:Number(id)},data:p.data}))}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;await prisma.material.delete({where:{id:Number(id)}});return NextResponse.json({ok:true})}
