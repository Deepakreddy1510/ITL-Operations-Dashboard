import {NextResponse} from "next/server";import {z} from "zod";import {prisma} from "@/lib/prisma";const schema=z.object({number:z.string().min(2),customerId:z.coerce.number().int(),items:z.string().min(2),quantity:z.coerce.number().positive(),orderDate:z.coerce.date(),deliveryDate:z.coerce.date(),amount:z.coerce.number().min(0),status:z.enum(["PENDING","PRODUCTION","PACKING","READY","DISPATCHED","DELIVERED","DELAYED","CANCELLED"]),notes:z.string().optional().nullable()});
export async function PUT(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:"Invalid order data"},{status:400});return NextResponse.json(await prisma.order.update({where:{id:Number(id)},data:p.data,include:{customer:true}}))}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;await prisma.order.delete({where:{id:Number(id)}});return NextResponse.json({ok:true})}
